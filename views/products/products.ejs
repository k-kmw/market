<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=category%></title>
</head>
<body>
    <h1><%=category%></h1>
    <ul class="products__container">
    <% products.forEach(product =>{   %> 
        <li id="<%=product._id%>"><a href="products/<%=product._id%>"><%=product.name%></a></li>
    <% }) %>  
    </ul>
    <div class="btn__container">
        <button class="new__btn"><a href="products/new">new</a></button>
        <% if(category !== 'All Products') {%> 
            <button class="all__btn"><a href="/products">All</a></button>
        <% } %> 
    </div>
</body>
</html>