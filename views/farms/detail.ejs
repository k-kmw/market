<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=farm.name%></title>
</head>
<body>
    <h1><%=farm.name%></h1>
    <ul>
        <li>city: <%=farm.city%></li>
        <li>email: <%=farm.email%></li>
    </ul>

    <h2>Products</h2>
    <ul>
        <% farm.products.forEach(product => { %>
            <li><a href="/products/<%=product._id%>"><%= product.name %> </a></li>
        <% })  %> 
    </ul>

    <div class="btn__container">
        <button class="home__btn"><a href="/farms">All Farms</a></button>
        <button><a href="/farms/<%= farm._id %>/products/new">Add Product</a></button>
    </div>
    <form action="/farms/<%=farm._id%>?_method=DELETE" method="POST">
        <button>delete</button>
    </form>
</body>
</html>